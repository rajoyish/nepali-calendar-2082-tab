{
  "name": "nepali-calendar-2082",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "copy-extension": "node scripts/copy-extension.js",
    "build-extension": "npm run build && npm run copy-extension",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
